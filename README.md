# mediator-middleware
Simple custom implementation of popular mediator pattern as middleware for your application. It helps map requests to its handlers, adding behaviors similar to ASP.Net middleware 
